import styled from "styled-components";

export const StyledHeader = styled.header`
    padding-block: 1rem;
    color: var(--color-white);
    background: var(--bg-lighter-neon);

`

export const StyledFooter = styled.footer`
    padding-block: 1rem;
    color: var(--color-white);
    background: var(--bg-lighter-neon);
    width: 100vw; //TEMP
    position: fixed; //TEMP
    bottom: 0;
    left: 0;
`